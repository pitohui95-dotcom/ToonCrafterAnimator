ToonCrafter 动画器（CPU 版 PyTorch）
==================================

独立 Windows 应用：用真实的 ToonCrafter 512 插帧模型插帧。
本 zip 捆绑的是 **CPU 版 PyTorch**，即使电脑有 NVIDIA GPU 也无法调用 CUDA。
GPU 插帧请使用 ToonCrafterAnimator-windows-x64-cuda.zip。

请整文件夹复制使用。双击 ToonCrafterAnimator.exe 即可启动。本应用不会下载权重。
