# GUI + inference Python deps (torch is separate: see requirements-torch.txt)
PySide6>=6.6
Pillow>=10.0
numpy>=1.26
omegaconf>=2.3
einops>=0.7
safetensors>=0.4
PyYAML>=6.0
tqdm>=4.65
pytorch-lightning>=2.0
# Exact pin from AIGODLIKE/ComfyUI-ToonCrafter @ 96024189. Newer open_clip
# defaults to Transformer batch_first=True; the vendored FrozenOpenCLIPEmbedder
# used to always permute NLD->LND, which makes CLIP's 77x77 attn_mask fail with
# "should be (1, 1)". condition.py also honors batch_first if this pin moves.
open_clip_torch==2.22.0
transformers>=4.36
kornia>=0.7
